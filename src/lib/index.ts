import { portTerminator, isPortOpen, portsTerminator } from "./terminator";

const terminator = {
  portTerminator,
  isPortOpen,
  portsTerminator,
};

export { portTerminator, isPortOpen, portsTerminator };

export default terminator;
